{% extends 'base.html.twig' %}

{% block title %}[TUTOMARKS] Connexion au site{% endblock %}

{% block body %}
    <div class="h-screen flex flex-col md:flex-row">
        <div class="flex pt-48 md:pt-0 w-full md:w-1/2 bg-gradient-to-tr from-secondary to-primary i justify-around items-center">
            <div class="px-10">
                <h1 class="text-white font-bold text-4xl font-sans">Connexion</h1>
                <p class="text-white text-2xl mt-3">Vous voulez partager des sources sur le site et en faire profiter d'autres développeurs ?</p>
                <img class="max-h-[300px] mt-10" src="{{ asset('/build/images/undraw_bg8.svg') }}" alt="" />
                <a href="#" class="block mt-10 text-end text-white mb-2">Politique de confidentialité de vos données personnelles</a>
            </div>
        </div>

        <div class="flex w-full pt-24 md:pt-0 md:w-1/2 justify-center items-center bg-white">
            <form method="post" class="bg-white" role="form" accept-charset="UTF-8">
                {% if error is defined and error is not null %}
                    <div class="alert alert-error mb-5">
                        <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                        <span>{{ error.message|trans }}</span>
                    </div>
                {% endif %}

                <div class="w-full flex justify-center">
                    <h1 class="text-neutral-800 font-bold text-3xl mb-3">Connexion</h1>
                </div>

                <label for="inputEmail" class="hidden" aria-hidden="true">Votre adresse mail</label>
                <div class="relative mb-6">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <svg class="w-5 h-5 text-gray-500 dark:text-gray-400" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"></path><path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"></path></svg>
                    </div>
                    <input type="email"
                           class="input-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full pl-10 p-2.5  dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
                           value="{{ last_username }}"
                           name="email"
                           id="inputEmail"
                           autocomplete="email"
                           required
                           placeholder="Votre adresse mail"
                           autofocus
                           />
                </div>

                <label for="inputPassword" class="hidden" aria-hidden="true">Mot de passe</label>
                <div class="relative mb-6">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <svg xmlns="http://www.w3.org/2000/svg"
                             class="h-5 w-5 text-gray-400"
                             viewBox="0 0 20 20"
                             fill="currentColor">
                            <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <input type="password"
                           class="input-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full pl-10 p-2.5  dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
                           name="password"
                           id="inputPassword"
                           autocomplete="current-password"
                           required
                           />
                </div>

                <input type="hidden"
                       name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                />

                <button type="submit"
                        class="block w-full bg-secondary hover:bg-primary mt-4 py-2 rounded-2xl text-white mb-4">Connexion</button>

                <div class="flex justify-end w-full">
                    <span class="text-end text-sm ml-2 bg-transparent hover:bg-primary hover:text-white text-neutral-400 transform p-1 -skew-y-6 inline-flex"><a class="transform skew-y-6 mx-3" href="#">Mot de passe oublié ?</a></span>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
